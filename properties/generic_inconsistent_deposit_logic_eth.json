{
    "event": "InconsistentDepositLogic(uint256,string,uint256)",
    "chain_name": "ethereum",
    "properties": { 
        "balance_after": "call(ethereum, eth_getBalance, [$payer_address, $ethereum_block_number.as(hex)]).get(result)",
        "balance_before": "call(ethereum, eth_getBalance, [$payer_address, ($ethereum_block_number - 1).as(hex)]).get(result)",
        "gas_amount": "call(ethereum, eth_getTransactionByHash, [$transaction_hash]).get(result).get(gas)",
        "gas_price": "call(ethereum, eth_getTransactionByHash, [$transaction_hash]).get(result).get(gasPrice)",
        "amount": "$event_data.slice(130,194).as(hex)"
    },
    "pattern": [
        "$value >= ($amount + (gas_price * gas_amount))"
    ]
}
